# Client environment variables (Vite)
# Copy to client/.env and replace values

# Google client id for Google Identity Services (GSI) and redirect flows
VITE_GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com

# API base (backend)
VITE_API_BASE=http://localhost:4500

# Optional: any other front-end flags
# VITE_SENTRY_DSN=

# Notes:
# - For local testing, make sure the SERVER's CLIENT_URL and this VITE_API_BASE match your local addresses.
# - GSI script will render a button using VITE_GOOGLE_CLIENT_ID. For redirect OAuth, server uses GOOGLE_CLIENT_ID/SECRET.
