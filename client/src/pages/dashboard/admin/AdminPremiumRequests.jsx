// Premium requests page removed — legacy placeholder kept to avoid build errors if referenced.
import React from 'react';

const AdminPremiumRequests = () => null;

export default AdminPremiumRequests;
